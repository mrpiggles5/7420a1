# Conference Room Reservation System

A full-stack web application built with **Django (backend)** and **React (frontend)** that allows users to log in, view available conference rooms, and manage reservations.

---

## 🧩 Features

- 🔐 **JWT Authentication** (via `rest_framework_simplejwt`)
- 🧾 **Room Listings** pulled dynamically from the Django REST API
- 💼 **User-based Reservations**
- 🌐 **CORS Configured** for multiple local hosts (3000–3002)
- ⚙️ **React Frontend + Django Backend Integration**

---

## 🗂️ Project Structure

